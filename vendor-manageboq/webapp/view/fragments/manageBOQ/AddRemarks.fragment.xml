<core:FragmentDefinition xmlns="sap.m" xmlns:core="sap.ui.core">
    <Dialog>
        <customHeader>
            <Bar>
                <contentLeft></contentLeft>
                <contentMiddle>
                    <Title text="{i18n>Addremark}"/>
                </contentMiddle>
                <contentRight></contentRight>
            </Bar>
        </customHeader>
        <content>
            <FlexBox renderType="Bare" wrap="Wrap">
                <TextArea id="idRemarksTextArea" value="{Remarks}" rows="3" width="100%" cols="80" class="sapUiSmallMargin"/>
            </FlexBox>
        </content>
        <beginButton>
            <Button text="{i18n>Add}" press="onAddRemarksPress" type="Emphasized"></Button>
        </beginButton>
        <endButton>
            <Button text="{i18n>Close}" press="onCloseRemarkPopupPress"></Button>
        </endButton>
    </Dialog>
</core:FragmentDefinition>