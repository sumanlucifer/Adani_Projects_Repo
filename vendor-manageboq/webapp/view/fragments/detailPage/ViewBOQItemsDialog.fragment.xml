<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Dialog >
        <content>
            <Table width="auto" growing="true" id="idViewBOQLineItemsTable" growingThreshold="5" items="{path: 'BOQItems'}" class="sapUiResponsiveMargin">
                <columns>
                    <Column>
                        <Text text="{i18n>ItemCode}" wrapping="true" wrappingType="Hyphenated"/>
                    </Column>
                    <Column>
                        <Text text="{i18n>ItemCode}" wrapping="true"/>
                    </Column>
                    <Column demandPopin="true" minScreenWidth="Tablet">
                        <Text text="{i18n>ItemDescription}"/>
                    </Column>
                    <Column demandPopin="true" minScreenWidth="Tablet" width="auto">
                        <Text text="{i18n>BOQQuantity}" />
                    </Column>
                    <Column demandPopin="true" minScreenWidth="Tablet">
                        <Text text="{i18n>UOM}"/>
                    </Column>
                </columns>
                <items>
                    <ColumnListItem>
                        <cells>
                            <ObjectIdentifier title="{MaterialCode}" titleActive="true" />
                            <Text text="{Name}"/>
                            <ExpandableText text="{Description}" maxCharacters="10" overflowMode="Popover"/>
                            <ObjectNumber number="{Qty}"/>
                            <Text text="{UOM}"/>
                        </cells>
                    </ColumnListItem>
                </items>
            </Table>
        </content>
        <endButton>
            <Button text="{i18n>Close}" press="onViewChildDialogClose" type="Emphasized" />
        </endButton>
    </Dialog>
</core:FragmentDefinition>
