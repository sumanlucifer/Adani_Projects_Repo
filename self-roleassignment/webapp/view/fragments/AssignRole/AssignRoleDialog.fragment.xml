<core:FragmentDefinition xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form">
    
     <Dialog >
        <content>
            <MultiComboBox id="roleEdit" placeholder= "Select" selectedKeys="{MasterRoleSet>/ID}"
                items="{
                    path: '/MasterRoleSet',
                    filters: [
                        {
                        filters: [
                            { path: 'ID', operator: 'NE', value1: '1'},
                            { path: 'ID', operator: 'NE', value1: '2'}
                        ],
                        and: true
                        }
                    ]
                }">
                <core:Item key="{ID}" text="{Role}"/> 
            </MultiComboBox>
        </content>
        <beginButton>
            <Button text="Save" press="onSave" type="Emphasized" />
        </beginButton>
        <endButton>
            <Button text="Close" press="onClose" type="Emphasized" />
        </endButton>
    </Dialog>

</core:FragmentDefinition>