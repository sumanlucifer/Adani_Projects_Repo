<mvc:View xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form"
    xmlns="sap.m"
    xmlns:smartTable="sap.ui.comp.smarttable"
    xmlns:tt="sap.ui.table" controllerName="com.agel.mmts.securityscanhistory.view.blocks.packingListDetails.innerPackaging.InnerPackagingController">


     <Table id="idInnerPackaging" items="{path: 'InnerPackagings'}" autoPopinMode="true" growingThreshold="5" growing="true" fixedLayout="false">
    
        <columns>
            <Column>
                <Text text="{i18n>InnerPackaging}"/>
            </Column>
            <Column>
                <Text text="{i18n>LineItem}"/>
            </Column>
            <!-- <Column>
                <Text text="Dispatch Qty."/>
            </Column> -->
            <Column>
                <Text text="{i18n>PackagingQty}"/>
            </Column>
            <Column>
                <Text text="{i18n>UOM}"/>
            </Column>
            <Column>
              <Text text="{i18n>Action}"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <ObjectIdentifier title="{PackagingType}"/>
                    <ObjectIdentifier title="{LineNumber}"/>
                    <!-- <Text text="{DispatchQty}"/> -->
                    <Text text="{PackagingQty}"/>
                    <Text text="{UOM}"/>
                    <Link text="{i18n>ViewQRCode}" binding="{QrCode}" visible="{= ${QRNumber}}" press="onViewQRCodePress"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>

    
</mvc:View>


