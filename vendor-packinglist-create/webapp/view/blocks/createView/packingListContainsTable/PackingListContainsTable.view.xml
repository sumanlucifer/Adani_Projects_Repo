<mvc:View xmlns:mvc="sap.ui.core.mvc"
    xmlns:core="sap.ui.core"
    xmlns:f="sap.ui.layout.form"
    xmlns="sap.m"
    xmlns:smartTable="sap.ui.comp.smarttable"
    xmlns:tt="sap.ui.table" controllerName="com.agel.mmts.vendorpackinglistcreate.view.blocks.createView.packingListContainsTable.PackingListContainsTableController">

    <Table id="idBOQItemTable" items="{path: 'packingListContainsModel>/items'}" autoPopinMode="true" growingThreshold="5" growing="true" fixedLayout="true">
        <headerToolbar>
            <OverflowToolbar >
                <content>
                    <ToolbarSpacer></ToolbarSpacer>
                    <Button text="Generate Packing List" press="onGeneratePackingList" visible="{= ${objectViewModel>/isPackingListInEditModel} ? true : false}"></Button>
                </content>
            </OverflowToolbar>
        </headerToolbar>
        <columns>
            <Column>
                <Text text="Material Code"/>
            </Column>
            <Column>
                <Text text="Name"/>
            </Column>
            <Column>
                <Text text="DispatchQty"/>
            </Column>
            <Column>
                <Text text="Order Unit"/>
            </Column>
            <Column>
                <Text text="Packaging Type"/>
            </Column>
            <Column width="8%">
                <Text text="No Of Packages"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <ObjectIdentifier title="{packingListContainsModel>LineNumber}"/>
                    <Text text="{packingListContainsModel>Name}"/>
                    <Text text="{packingListContainsModel>DispatchQty}" />
                    <Text text="{packingListContainsModel>UOM}"/>
                    <Select id="idPackagingTypeSelect" forceSelection="false" selectedKey="{packingListContainsModel>PackagingTypeId}" selectedItem="{packingListContainsModel>PackagingType}" items="{
                             path: '/MasterPackagingTypeSet',
                             templateShareable:false
                            }" change="onPackingListTypeChange" editable="{= ${objectViewModel>/isPackingListInEditModel} ? true : false}">
                        <core:Item key="{ID}" text="{Name}" />
                    </Select>
                    <Input value="{packingListContainsModel>NumberOfPackages}" editable="{= ${objectViewModel>/isPackingListInEditModel} ? true : false}" type="Number"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>

    <Table id="idPackagingTable" items="{path: 'InnerPackagingModel>/items'}" autoPopinMode="true" growingThreshold="5" growing="true" fixedLayout="true" class="sapUiLargeMarginTop">
        <!-- <headerToolbar>
            <OverflowToolbar >
                <content>
                    <ToolbarSpacer></ToolbarSpacer>
                    <Button text="Generate Packing List" press="onGeneratePackingList"></Button>
                </content>
            </OverflowToolbar>
        </headerToolbar> -->
        <columns>
            <Column>
                <Text text="Packaging Type"/>
            </Column>
            <Column>
                <Text text="Name"/>
            </Column>
            <Column >
                <Text text="Packaging Quantity"/>
            </Column>
            <Column>
                <Text text="UOM"/>
            </Column>
        </columns>
        <items>
            <ColumnListItem>
                <cells>
                    <ObjectIdentifier title="{InnerPackagingModel>PackagingType}"/>
                    <Text text="{InnerPackagingModel>LineNumber}"/>
                    <Input value="{InnerPackagingModel>PackagingQty}" width="50%" editable="{= ${objectViewModel>/isPackingListInEditModel} ? true : false}"/>
                    <Text text="{InnerPackagingModel>UOM}"/>
                </cells>
            </ColumnListItem>
        </items>
    </Table>

</mvc:View>


